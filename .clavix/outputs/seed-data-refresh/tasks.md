# Implementation Plan

**Project**: seed-data-refresh
**Generated**: 2026-03-17T14:55:00Z

## Technical Context & Standards
*Detected Stack & Patterns*
- **Architecture**: Python/Flask monolith with bash startup scripts
- **Framework**: Flask (src/imageset_generator/app.py)
- **Startup**: `.venv/bin/python`, PYTHONPATH and IMAGESET_GENERATOR_ROOT env vars
- **Scripts**: `scripts/` directory (start-web.sh, start-dev.sh, etc.)
- **Conventions**: `.venv` virtual env, `set -e` in scripts, seed data in `src/imageset_generator/data/`
- **API Endpoints**: POST `/api/versions/refresh`, POST `/api/channels/refresh`, POST `/api/releases/refresh` (requires version+channel params)
- **JSON Structures**:
  - `ocp-versions.json`: `{"releases": [...], "count": N, ...}`
  - `ocp-channels.json`: `{"channels": {"4.16": [...], ...}, "count": N, ...}`
  - `channel-releases.json`: `{"channel_releases": {"stable-4.16": [...], ...}, "count": N, ...}`

---

## Phase 1: Script Skeleton and App Lifecycle

- [ ] **Create script with app start/stop lifecycle** (ref: PRD - Workflow)
  Task ID: phase-1-skeleton-01
  > **Implementation**: Create `scripts/refresh-seed-data.sh`.
  > **Details**: Bash script with `set -euo pipefail`. Set up env vars matching `scripts/start-web.sh` (PYTHONPATH, IMAGESET_GENERATOR_ROOT). Start the Flask app in background on a free port (default 5001 to avoid conflicts with running dev server). Use a trap on EXIT to kill the app process. Wait for readiness by polling `/api/health` or the root URL with `curl --retry` before proceeding. Must use `.venv/bin/python` (never system python). Make script executable.

---

## Phase 2: Refresh Endpoint Calls

- [ ] **Call refresh endpoints in dependency order** (ref: PRD - Workflow steps 2-3)
  Task ID: phase-2-refresh-01
  > **Implementation**: Add refresh functions to `scripts/refresh-seed-data.sh`.
  > **Details**: Call endpoints via `curl` in this order:
  > 1. `POST /api/versions/refresh` — populates `data/ocp-versions.json`
  > 2. `POST /api/channels/refresh` — populates `data/ocp-channels.json` (reads versions file internally to iterate all versions)
  > 3. For channel-releases: parse the refreshed `data/ocp-channels.json` to extract all unique channel names across all versions, then for each channel call `POST /api/releases/refresh` with version and channel params. Note: the `/api/releases/refresh` endpoint requires both `version` and `channel` — extract these from the channels data (each version maps to a list of channels).
  > Check HTTP status codes; log and track per-endpoint success/failure. Continue attempting remaining endpoints even if one fails.

---

## Phase 3: Validation

- [ ] **Validate refreshed data against current seed data** (ref: PRD - Validation)
  Task ID: phase-3-validation-01
  > **Implementation**: Add validation functions to `scripts/refresh-seed-data.sh`.
  > **Details**: For each of the three files, compare counts between current seed (`src/imageset_generator/data/`) and refreshed runtime cache (`data/`):
  > - `ocp-versions.json`: count of `.releases` array entries (use `jq '.releases | length'`)
  > - `ocp-channels.json`: count of `.channels` keys (use `jq '.channels | keys | length'`)
  > - `channel-releases.json`: count of `.channel_releases` keys (use `jq '.channel_releases | keys | length'`)
  > Reject (set failure flag) if `new_count < current_count`. Print per-file result: `"ocp-versions.json: 23 -> 25 OK"` or `"ocp-versions.json: 23 -> 1 REJECTED"`. Require `jq` as a dependency (check at script start).

---

## Phase 4: Copy and Summary

- [ ] **Copy validated files and print summary** (ref: PRD - Workflow step 4)
  Task ID: phase-4-copy-01
  > **Implementation**: Add copy and summary logic to `scripts/refresh-seed-data.sh`.
  > **Details**: Only copy files that passed validation from `data/` to `src/imageset_generator/data/`. Skip files that failed validation. Print a summary table showing: filename, old count, new count, status (UPDATED / SKIPPED / FAILED). Exit with non-zero code if any file was rejected or any refresh endpoint failed. On success, remind the user to review and commit the changes.

---

*Generated by Clavix /clavix:plan*
